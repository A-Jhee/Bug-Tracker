CREATE TABLE projects (
  id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  name varchar(60) UNIQUE NOT NULL,
  description varchar(270) NOT NULL
);

INSERT INTO projects (name, description)
  VALUES ('bug tracker', 'Application to track bugs across different priorities and statuses'), 
         ('Personal Finance Manager', 'Application to set budget and track spendings and earnings');